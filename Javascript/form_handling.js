/*--------------------Converts the form data into a JSON object to send to AWS Lambda-------------------*/
/*----------------------Also handles incorrect input and validates the data submited--------------------*/

/*----------------------------Upload Menu Form------------------------------*/

/*jQuery(document).on('ready', function() {
    jQuery('#upload_menu').bind('submit', function(event){
		event.preventDefault;
        
    });
});*/